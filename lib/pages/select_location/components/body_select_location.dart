import 'package:flutter/material.dart';

class BodySelectLocation extends StatelessWidget {
  const BodySelectLocation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
